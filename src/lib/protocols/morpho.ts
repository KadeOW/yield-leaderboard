import type { Position } from '@/types';

/**
 * Morpho position reader.
 * Stub — will be implemented when Morpho deploys on MegaETH.
 */
export async function getMorphoPositions(address: string): Promise<Position[]> {
  void address;
  return [];
}
